import { useState } from 'react'
import { observer } from 'mobx-react-lite'
import { {{pascalCase name}}Store } from "./{{pascalCase name}}Store";
import  "./{{pascalCase name}}.scss";

export interface {{pascalCase name}}Props {
 className?: string;
}

const {{pascalCase name}} = ({ className="" }:{{pascalCase name}}Props) => {
  const [{{camelCase name}}Store] = useState(() => new {{pascalCase name}}Store());

  return (
    <div className={`{{camelCase name}}`}>
      {{pascalCase name}} component
    </div>
  );
};

export default observer({{pascalCase name}});