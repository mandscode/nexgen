{
  "name": "nexgen-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix evolve\" \"npm run dev --prefix backend\" \"npm run dev --prefix admin\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
